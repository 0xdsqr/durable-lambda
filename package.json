{
  "name": "durable-lambda",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "release": "bun run build --filter=@dsqr/durable-lambda && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "2.29.7",
    "@tsconfig/node22": "22.0.0",
    "@types/bun": "latest",
    "typescript": "5.6.3"
  }
}
